[
  # set of input files and directories to apply formatting to
  inputs: [
    "{mix,.credo,.formatter}.exs",
    "{benchmarks,lib,scripts,test}/**/*.{ex,exs}"
  ],

  # match the Credo linter
  line_length: 100
]
